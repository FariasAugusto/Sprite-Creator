import React from "react";
import './styles.js';

const TituloInstructions = (props) => (
    
    <TituloInstructions>

        {props.children}

    </TituloInstructions>

);

export default TituloInstructions;